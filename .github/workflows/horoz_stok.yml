name: Horoz Stok Güncelle

on:
  schedule:
    - cron: '0,15,30,45 6-19 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  stok-guncelle:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu çek
        uses: actions/checkout@v4

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Bağımlılıkları yükle
        run: |
          pip install playwright requests
          playwright install chromium

      - name: Stok çek
        env:
          HOROZ_KULLANICI: ${{ secrets.HOROZ_KULLANICI }}
          HOROZ_SIFRE: ${{ secrets.HOROZ_SIFRE }}
        run: python horoz_stok_actions.py

      - name: Log göster
        if: always()
        run: cat horoz_log.txt || echo "Log dosyası yok"

      - name: stok.json'ı commit et
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add stok.json
          git diff --staged --quiet || git commit -m "stok güncellendi"
          git push
